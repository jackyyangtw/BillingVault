/**
 * 認證模組類型定義
 *
 * 定義可安全傳遞到前端 UI 的認證相關 TypeScript 類型。
 */

// ---------------------------------------------------------------------------
// User
// ---------------------------------------------------------------------------

/** 前端 UI 使用的用戶資料（非敏感） */
export interface UserProfile {
  id: string;
  name: string;
  email: string;
  phone?: string;
  avatar?: string;
}
